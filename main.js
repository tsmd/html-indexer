// src ディレクトリの設定
const src = 'src/articles'

// base ディレクトリの設定
const base = 'src'

// files テンプレートと対象ファイル群のマッピング
const files = {
  index: {
    files: '*.hbs'
  },
  tags: {
    files: 'tags/**/*.hbs'
  }
}

// files 繰り返す

  // glob

  // 各ファイル 繰り返す

    // タグを取得

    // カテゴリを取得

    // 日付を取得

    // タイトルを取得

    // 概要文を取得

    //
