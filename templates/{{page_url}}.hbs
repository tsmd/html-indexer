<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Index</title>
</head>
<body>

<h2>Articles ({{page}} of {{page_max}})</h2>

<ul>
  {{#each contextEntries}}
  <li>
    <article class="entry">
      <h3><a href="/{{file}}">{{title}}</a></h3>
      <time datetime="{{date}}">{{date}}</time>
      <p>{{description}}</p>
      <p>Tags: {{#each tags}}{{#unless @first}}, {{/unless}}<a href="/tags/{{tag_url}}/index.html">{{tag}}</a>{{/each}}</p>
      <p>Category: <a href="/categories/{{category.category_url}}/index.html">{{category.category}}</a></p>
    </article>
  </li>
  {{/each}}
</ul>

<p>
  {{page}} / {{page_max}}
</p>

<hr>

<h2>Tags</h2>
<ul>
  {{#each tags}}
  <li><a href="/tags/{{tag_url}}/index.html">{{tag}}</a></li>
  {{/each}}
</ul>

<h2>Categories</h2>
<ul>
  {{#each categories}}
  <li><a href="/categories/{{category_url}}/index.html">{{category}}</a></li>
  {{/each}}
</ul>

<h2>Monthly Archives</h2>
<ul>
</ul>

</body>
</html>
